image: ahmedmorad101/openjdk23-maven


stages:
- test

junit_tests:
  stage: test
  script:
    - mvn test
  tags:
    - docker
  artifacts:
    reports:
      junit:
        - target/surefire-reports/*.xml

